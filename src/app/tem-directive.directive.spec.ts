import { TemDirectiveDirective } from './tem-directive.directive';

describe('TemDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TemDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
